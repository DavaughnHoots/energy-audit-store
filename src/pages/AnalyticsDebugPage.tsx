import React from 'react';
import AnalyticsDebugger from '@/components/analytics/AnalyticsDebugger';

/**
 * Page wrapper for the analytics debugger component
 * Access this page at /analytics-debug in the browser
 */
const AnalyticsDebugPage: React.FC = () => {
  return <AnalyticsDebugger />;
};

export default AnalyticsDebugPage;
